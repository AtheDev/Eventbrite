<!DOCTYPE html>
<html>
<head>
  <title></title>
  <style type="text/css">

  .card-body {
    padding: 0.80rem;
  }
  .customc {
    min-width: 280px;
  }
  a.customc,
  a.customc:hover {
    color: inherit;
    text-decoration: none;
  }
  .col {
    min-width: 240px;
  }
  .custom-card:hover {
    box-shadow: 0 0 11px rgba(33, 33, 33, .2)
  }
  .custom-card{
    max-height: 370px;
    min-height: 280px;
  }
  h6.card-text{
    overflow: hidden;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
  }
  .card-title{
    font-size: 13px;
  }
  .card-img-top {
    object-fit: cover; height: 130px;
  }

  </style>
</head>
<body>

    <div class="container mt-5">
      <div class="d-flex justify-content-center align-items-center">
        <h2 class="text-black">BIENVENUE!</h2>
      </div>
    </div>

<div class="container mt-5">
  <div class="row row-cols-1 row-cols-md-4">
    <% @event.order([:id]).each do |event| %>
        <% @id = Event.where(title: "#{event.title}").ids %>
        <a href="<%= event_path(@id) %>" class="customc">
          <div class="col mb-4">
            <div class="card h-100 custom-card">
              <% if event.picture.attached? %>
              <%= image_tag event.picture, alt: 'picture', class: "card-img-top" %>
              <% else %>
              <%= image_tag asset_path('img_event.jpg'), alt: 'picture', class: "card-img-top" %>
              <% end %>
              <div class="card-body">
                <p class="card-title text-danger"><%= event.start_date.strftime("%a %m %Y") %></p>
                <h6 class="card-text"><%= event.title %></h6>
              </div>
            </div>
          </div>
        </a>
    <% end %>
  </div>
</div>

</body>
</html>
